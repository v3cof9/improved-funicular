name: Spotify Downloader

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Spotify URL to download'
        required: true
        type: string

permissions:
  contents: write

jobs:
  download:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python and install spotify-downloader
        run: |
          python -m pip install --upgrade pip
          pip install spotify-downloader
          python -m spotify_downloader --help

      - name: Download Spotify track
        run: |
          mkdir -p downloads
          python -m spotify_downloader "${{ inputs.url }}" \
            --output "downloads/"

      - name: Debug - Check what was downloaded
        run: |
          echo "=== Downloads folder ==="
          ls -la downloads/ || echo "No downloads folder"
          echo "=== Git status ==="
          git status

      - name: Commit files to repo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add downloads/
          git status
          git commit -m "Add downloaded Spotify track" || echo "No changes to commit"
          git push origin HEAD:main || git push origin HEAD:master
